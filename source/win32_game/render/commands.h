#pragma once

namespace render
{
	/*
		Here we define commands that go into a present queue,
		basically an abstraction of rendering anything for the game update_frame 
		system.

		So it should be able produce any effect needed in the game. This does not define
		how to aquire resource, but just how to put them together in a way that can produce
		a pretty image in the end.
	*/
	namespace command
	{

	}

	
}